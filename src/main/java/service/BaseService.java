package service;
/*
import java.sql.Connection;

import repository.ProjectRepository;
import repository.TaskRepository;
import repository.UserRepository;

//Bỏ nó đi, bữa anh làm thấy nó dư

public abstract class BaseService {
	//Mỗi thằng làm 1 chức năng thôi để dễ control
	protected UserRepository _userRepository;
	protected ProjectRepository _projectRepository;
	protected TaskRepository _taskRepository;
	
	protected Connection _connection;
	public BaseService(Connection connection) {
		_connection = connection;
		_userRepository = new UserRepository();
		_projectRepository = new ProjectRepository();
		_taskRepository = new TaskRepository();
	}
}
*/
